import { useMemo, useState } from "react";
import { SlidersHorizontal } from "lucide-react";
import { products } from "@/lib/catalog";
import { ProductCard } from "./product-card";
import { Button } from "@/components/ui/button";
export function CatalogPage({category,title,intro}:{category?:string;title:string;intro:string}){const [size,setSize]=useState("Todos");const [color,setColor]=useState("Todas");const [sort,setSort]=useState("featured");const [filters,setFilters]=useState(false);const list=useMemo(()=>{let r=products.filter(p=>(!category||p.category===category)&&(size==="Todos"||p.sizes.includes(size))&&(color==="Todas"||p.color===color));return [...r].sort((a,b)=>sort==="low"?a.price-b.price:sort==="high"?b.price-a.price:Number(a.id)-Number(b.id))},[category,size,color,sort]);return <main><section className="border-b border-border bg-card py-14 text-center"><p className="eyebrow">Água Limpa</p><h1 className="mt-3 font-serif text-5xl md:text-6xl">{title}</h1><p className="mx-auto mt-4 max-w-xl text-sm leading-6 text-muted-foreground">{intro}</p></section><section className="section-shell py-10"><div className="mb-8 flex flex-wrap items-center justify-between gap-3 border-b border-border pb-5"><Button variant="outline" onClick={()=>setFilters(!filters)}><SlidersHorizontal/> Filtrar</Button><p className="text-xs text-muted-foreground">{list.length} peças</p><select value={sort} onChange={e=>setSort(e.target.value)} className="h-10 border border-border bg-card px-3 text-sm"><option value="featured">Destaques</option><option value="low">Menor preço</option><option value="high">Maior preço</option></select></div>{filters&&<div className="mb-8 grid gap-5 bg-card p-5 sm:grid-cols-2"><label className="text-xs uppercase tracking-wider">Tamanho<select value={size} onChange={e=>setSize(e.target.value)} className="mt-2 block h-10 w-full border border-border bg-background px-3"><option>Todos</option>{["P","M","G","GG","Único"].map(x=><option key={x}>{x}</option>)}</select></label><label className="text-xs uppercase tracking-wider">Cor<select value={color} onChange={e=>setColor(e.target.value)} className="mt-2 block h-10 w-full border border-border bg-background px-3"><option>Todas</option>{[...new Set(products.map(p=>p.color))].map(x=><option key={x}>{x}</option>)}</select></label></div>}{list.length?<div className="grid grid-cols-2 gap-x-4 gap-y-10 lg:grid-cols-4">{list.map(p=><ProductCard key={p.id} product={p}/>)}</div>:<div className="py-24 text-center"><h2 className="text-3xl">Nenhuma peça por aqui</h2><p className="mt-2 text-muted-foreground">Experimente alterar os filtros.</p></div>}</section></main>}
