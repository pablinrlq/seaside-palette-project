import { createFileRoute, Link } from "@tanstack/react-router";
import { Minus, Plus, Trash2 } from "lucide-react";
import { useState } from "react";
import { Button } from "@/components/ui/button";import { Input } from "@/components/ui/input";import { money } from "@/lib/catalog";import { useStore } from "@/components/store/store-context";
export const Route=createFileRoute("/sacola")({head:()=>({meta:[{title:"Sua sacola — Água Limpa Beachwear"},{name:"description",content:"Revise suas escolhas antes de finalizar."},{property:"og:title",content:"Sua sacola — Água Limpa Beachwear"},{property:"og:description",content:"Revise suas escolhas antes de finalizar."},{property:"og:type",content:"website"},{name:"twitter:card",content:"summary_large_image"}]}),component:Cart});
function Cart(){const {items,remove,setQuantity,subtotal}=useStore();const [cep,setCep]=useState("");const [shipping,setShipping]=useState(false);if(!items.length)return <main className="section-shell py-24 text-center"><h1 className="text-5xl">Sua sacola está leve</h1><p className="mt-4 text-muted-foreground">Escolha peças para acompanhar seus próximos dias de sol.</p><Button asChild className="mt-8"><Link to="/colecao">Conhecer a coleção</Link></Button></main>;return <main className="section-shell py-12"><h1 className="mb-10 text-5xl">Sua sacola</h1><div className="grid gap-12 lg:grid-cols-[1fr_380px]"><div className="divide-y divide-border">{items.map(x=><article key={x.product.id+x.size} className="flex gap-5 py-6"><img src={x.product.image} alt={x.product.name} className="h-36 w-28 object-cover"/><div className="flex flex-1 flex-col"><div className="flex justify-between"><div><h2 className="text-2xl">{x.product.name}</h2><p className="text-sm text-muted-foreground">{x.product.color} · {x.size}</p></div><Button variant="ghost" size="icon" onClick={()=>remove(x.product.id)}><Trash2/></Button></div><div className="mt-auto flex items-end justify-between"><div className="flex items-center border border-border"><Button variant="ghost" size="icon" onClick={()=>setQuantity(x.product.id,x.quantity-1)}><Minus/></Button><span>{x.quantity}</span><Button variant="ghost" size="icon" onClick={()=>setQuantity(x.product.id,x.quantity+1)}><Plus/></Button></div><strong>{money(x.product.price*x.quantity)}</strong></div></div></article>)}</div><aside className="h-fit bg-card p-6"><h2 className="text-3xl">Resumo</h2><div className="mt-6 flex justify-between border-b border-border pb-4"><span>Subtotal</span><strong>{money(subtotal)}</strong></div><label className="mt-5 block text-xs uppercase">Cupom<Input className="mt-2" placeholder="Digite seu cupom"/></label><label className="mt-5 block text-xs uppercase">Calcular frete<div className="mt-2 flex gap-2"><Input value={cep} onChange={e=>setCep(e.target.value)} placeholder="00000-000"/><Button variant="outline" onClick={()=>setShipping(true)}>Calcular</Button></div></label>{shipping&&<p className="mt-3 text-sm text-primary">Entrega estimada: 4–7 dias úteis</p>}<div className="mt-6 flex justify-between text-lg"><span>Total</span><strong>{money(subtotal)}</strong></div><Button asChild size="lg" className="mt-6 w-full"><Link to="/checkout">Continuar para checkout</Link></Button></aside></div></main>}
